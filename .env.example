# ══════════════════════════════════════════════════════════════
# .env 模板 —— 复制为 .env 后填入实际值。pi 求解器从**仓库根目录**读它。
#
#   cp .env.example .env
#   go run ./example          # 或你自己的调用方
#
# 这个文件里的变量会被**原样注入 pi 子进程的环境**，pi 自己认它们。
# 仓库代码不读 key 的值，只负责转发。
# ══════════════════════════════════════════════════════════════

# ── 凭据 ──
# 变量名是 pi 的官方惯例：<PROVIDER 大写，连字符换下划线>_API_KEY。
#   未知名 provider 也照此推导，不需要在代码里登记。
#   对照表：https://pi.dev/docs/latest/providers
# 例：
#   opencode-go → OPENCODE_API_KEY
#   deepseek    → DEEPSEEK_API_KEY
#   anthropic   → ANTHROPIC_API_KEY
OPENCODE_API_KEY=sk-replace-with-your-key

# ── 提供商与模型 ──
# PI_PROVIDER 是 pi 的 --provider 值；PI_MODEL 是 --model 值。
# 两个都留空则用 pi 自己的默认值（provider=google）。
# 可用值见 `pi --list-models`。
PI_PROVIDER=opencode-go
PI_MODEL=deepseek-v4-flash
